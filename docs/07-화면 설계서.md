# 07. 화면 설계서

> **버전**: v1.2 (S03 상태별 버튼 분기 추가, 채팅 정책 명시, 예약 버튼 UX 명시)
> **대상**: 프론트엔드 바이브코딩 참고용
> **형식**: 텍스트 와이어프레임 + 화면별 API 매핑

---

## 화면 목록

| # | 화면명 | 경로 | 인증 필요 |
|---|--------|------|-----------|
| S01 | 홈 (상품 목록) | `/` | ❌ |
| S02 | 상품 검색 | `/search` | ❌ |
| S03 | 상품 상세 | `/products/{id}` | ❌ |
| S04 | 상품 등록 | `/products/new` | 🔒 |
| S05 | 상품 수정 | `/products/{id}/edit` | 🔒 |
| S06 | 채팅방 목록 | `/chat` | 🔒 |
| S07 | 채팅방 상세 | `/chat/{chatRoomId}` | 🔒 |
| S08 | 마이페이지 | `/my` | 🔒 |
| S09 | 내 판매 목록 | `/my/products` | 🔒 |
| S10 | 내 찜 목록 | `/my/likes` | 🔒 |
| S11 | 로그인 | `/login` | ❌ |
| S12 | 회원가입 | `/signup` | ❌ |
| S13 | 리뷰 작성 | `/trades/{id}/review` | 🔒 |

---

## S01. 홈 — 상품 목록

```
┌─────────────────────────────────┐
│  🏠 마켓플레이스          👤 [로그인] │
├─────────────────────────────────┤
│  🔍 [검색창: 키워드를 입력하세요]    │
├─────────────────────────────────┤
│  카테고리: [전체▼] [가구] [전자기기]  │
│            [의류] [도서] [기타]      │
├─────────────────────────────────┤
│ ┌────────┐ ┌────────┐           │
│ │[이미지]│ │[이미지]│           │
│ │침대프레임│ │아이패드  │           │
│ │150,000 │ │200,000 │           │
│ │판매중  │ │예약중  │           │
│ │♥ 7     │ │♥ 12    │           │
│ └────────┘ └────────┘           │
│ ┌────────┐ ┌────────┐           │
│ │[이미지]│ │[이미지]│           │
│ │...     │ │...     │           │
│ └────────┘ └────────┘           │
│           [더 보기 ↓]            │
├─────────────────────────────────┤
│ 🏠홈  🔍검색  ✉️채팅  👤마이        │
└─────────────────────────────────┘
```

**연결 API**: `GET /products?cursor=&size=20`
**동작**:
- 무한 스크롤 (Cursor 페이지네이션)
- 카테고리 탭 클릭 → `GET /products/search?categoryId=`
- 검색창 클릭 → S02로 이동

---

## S02. 상품 검색

```
┌─────────────────────────────────┐
│  ← 검색                          │
├─────────────────────────────────┤
│  🔍 [침대 프레임          ] ✕   │
├─────────────────────────────────┤
│  인기 검색어                     │
│  1. 침대 프레임  2. 아이패드       │
│  3. 자전거      4. 원피스         │
│  5. 책상        6. 소파           │
├─────────────────────────────────┤
│  카테고리 필터: [전체▼]           │
├─────────────────────────────────┤
│  검색 결과 12건                   │
│ ┌──────────────────────────────┐ │
│ │[썸네일] 침대 프레임 퀸사이즈    │ │
│ │         150,000원 · 판매중    │ │
│ │         ♥ 7                  │ │
│ └──────────────────────────────┘ │
│ ┌──────────────────────────────┐ │
│ │[썸네일] 침대 프레임 싱글      │ │
│ │         80,000원 · 판매중     │ │
│ └──────────────────────────────┘ │
└─────────────────────────────────┘
```

**연결 API**:
- `GET /search/popular` (인기 검색어, Caffeine 캐시)
- `GET /products/search?keyword=침대+프레임&categoryId=`

---

## S03. 상품 상세

```
┌─────────────────────────────────┐
│  ←                        ⋮     │
├─────────────────────────────────┤
│  ┌───────────────────────────┐  │
│  │                           │  │
│  │        [상품 이미지]       │  │
│  │                           │  │
│  └───────────────────────────┘  │
│   ●  ○  ○  (이미지 인디케이터)   │
├─────────────────────────────────┤
│  [판매중] 가구 > 침대/매트리스    │
│  침대 프레임 퀸사이즈             │
│  150,000원                      │
│  등록일: 2025-06-01             │
├─────────────────────────────────┤
│  판매자 정보                     │
│  👤 김민준  ⭐ 4.8 (리뷰 23개)   │
├─────────────────────────────────┤
│  상품 설명                       │
│  2년 사용, 상태 양호합니다.       │
│  이사 때문에 급처합니다...        │
├─────────────────────────────────┤
│  ♥ 찜하기(7)    [채팅하기]       │
│              [예약하기]          │
└─────────────────────────────────┘
```

**연결 API**: `GET /products/{productId}`

**상태별 하단 버튼 분기표**:

| PRODUCT.status | 구매자(타인) 화면 | 판매자(본인) 화면 |
|----------------|-----------------|-----------------|
| SALE | [채팅하기] [예약하기 →] | [수정] [삭제] |
| RESERVED | [채팅하기] [예약불가 — 이미 예약됨 (disabled)] | [거래시작] [예약취소] |
| TRADING | [채팅하기] [거래진행중 (disabled)] | [거래완료] |
| SOLD | [채팅하기] — | [판매완료] |
| DELETED | 접근 불가 (404) | — |

> **찜하기**: SALE · RESERVED · TRADING 상태에서 활성. SOLD · DELETED 상태에서는 비활성(숨김). 예약 취소 시 관심 등록자가 재시도할 수 있도록 예약중에도 찜 허용.

**동작 상세**:

- **[채팅하기]**: 비로그인 시 → 로그인 화면 이동. 로그인 상태 → `POST /chat-rooms` 호출 → 채팅방 진입 (기존 채팅방 있으면 재진입)
- **[예약하기]**: 클릭 즉시 버튼 `disabled` 처리 (중복 클릭 방지) → `POST /products/{productId}/reserve`
  - 성공(200): "예약되었습니다!" 토스트 → 현재 상품 상세 유지 (채팅 필요 시 [채팅하기] 버튼으로 별도 진입)
  - 실패(409 `ALREADY_RESERVED`): "이미 예약된 상품입니다." 토스트 → `retryAfter`초 후 상품 상세 재조회
  - 버튼은 응답 수신 후 재활성화
- **[찜하기]**: `POST /products/{productId}/likes` → 숫자 +1 즉시 반영 (낙관적 업데이트)
- **판매자 본인**: 예약/찜 버튼 숨김, 수정/삭제/거래관리 버튼 표시

---

## S04. 상품 등록

```
┌─────────────────────────────────┐
│  ← 상품 등록                [완료] │
├─────────────────────────────────┤
│  사진 추가 (최대 10장)           │
│  ┌──┐ ┌──┐ ┌──┐               │
│  │📷+│ │  │ │  │               │
│  └──┘ └──┘ └──┘               │
├─────────────────────────────────┤
│  카테고리 *                      │
│  [가구       ▼] [침대/매트리스▼] │
├─────────────────────────────────┤
│  제목 * (2~40자)                 │
│  ┌─────────────────────────┐    │
│  │                         │    │
│  └─────────────────────────┘    │
├─────────────────────────────────┤
│  가격 *                          │
│  ┌─────────────────────────┐    │
│  │                    원   │    │
│  └─────────────────────────┘    │
├─────────────────────────────────┤
│  설명 (최대 1000자)              │
│  ┌─────────────────────────┐    │
│  │                         │    │
│  │                         │    │
│  └─────────────────────────┘    │
└─────────────────────────────────┘
```

**연결 API**: `POST /products`

---

## S07. 채팅방 상세

```
┌─────────────────────────────────┐
│  ← 김민준                   ⋮   │
│     침대 프레임 · 150,000원       │
├─────────────────────────────────┤
│                                  │
│         2025-06-01               │
│                                  │
│  ┌──────────────────┐           │
│  │ 아직 판매 중인가요?│           │
│  └──────────────────┘  이서연    │
│                                  │
│  김민준 ┌────────────────────┐  │
│         │네, 판매 중입니다 :) │  │
│         └────────────────────┘  │
│                                  │
│  ┌──────────────────┐           │
│  │ 예약 가능할까요?   │           │
│  └──────────────────┘           │
│                                  │
├─────────────────────────────────┤
│  [상품 예약하기 →]               │
├─────────────────────────────────┤
│ ┌─────────────────────┐  [전송] │
│ │ 메시지를 입력하세요   │         │
│ └─────────────────────┘         │
└─────────────────────────────────┘
```

**연결 API**:
- `GET /chat-rooms/{chatRoomId}/messages` (이전 메시지 REST 조회)
- WebSocket STOMP 연결: 실시간 메시지 송수신
- [상품 예약하기] → `POST /products/{productId}/reserve`

---

## S08. 마이페이지

```
┌─────────────────────────────────┐
│  마이페이지                       │
├─────────────────────────────────┤
│  👤 김민준                       │
│  ⭐ 4.8 · 리뷰 23개              │
│  [프로필 수정]                   │
├─────────────────────────────────┤
│  🔒 비밀번호 변경     >          |
│  📦 내 판매 목록     >          |
│  🛒 내 구매 목록     >          |
│  ♥  내 찜 목록       >          |
│  ✉️  채팅            >          |
├─────────────────────────────────┤
│  [로그아웃]                      │
└─────────────────────────────────┘
```


**연결 API**:
- 프로필 조회: `GET /members/me` (닉네임, 평점, 리뷰 수)
- 프로필 수정: `PATCH /members/me` (닉네임, 프로필 이미지)
- 로그아웃: `POST /auth/logout` → 로컬 토큰 제거 → 로그인 화면 이동
- 비밀번호 변경: `PATCH /members/me/password`  → 클릭 시 모달 or 별도 페이지
**동작**:
- [프로필 수정] 클릭 → S08-1 프로필 수정 화면 이동
- [비밀번호 변경] 클릭 → S08-2 비밀번호 변경 화면 또는 모달 오픈


---
## S08-1. 프로필 수정

```
┌─────────────────────────────────┐
│ ← 프로필 수정 [저장]              │
├─────────────────────────────────┤
│ 프로필 이미지                     │
│ ┌───────────────┐               │
│ │ [미리보기]    │               │
│ └───────────────┘               │
│ [이미지 업로드] [이미지 삭제]     │
├─────────────────────────────────┤
│ 닉네임 *                        │
│ ┌─────────────────────────┐     │
│ │ 김민준                   │     │
│ └─────────────────────────┘     │
├─────────────────────────────────┤
│ ┌─────────────────────────┐     │
│ │ 저장                    │     │
│ └─────────────────────────┘     │
└─────────────────────────────────┘
```
**연결 API**:
- 초기 데이터: `GET /members/me`
- 저장: `PATCH /members/me`

**동작 상세**:
- 기존 프로필 이미지가 있으면 미리보기 표시
- [이미지 업로드] 클릭 시 새 이미지 선택
- [이미지 삭제] 클릭 시 기본 프로필 이미지 상태로 변경
- 저장 시 닉네임 + 프로필 이미지 정보 함께 반영
- 새 이미지 업로드 없이 저장하면 기존 이미지 유지
- 이미지 삭제 후 저장하면 프로필 이미지 제거 반영

**예외 처리**:
- 닉네임 유효성 오류 → 인라인 메시지 표시
- 이미지 업로드 실패 → 토스트 "이미지 업로드에 실패했습니다. 다시 시도해주세요."

---
## S08-2. 비밀번호 변경

```
┌─────────────────────────────────┐
│ ← 비밀번호 변경 [완료]           │
├─────────────────────────────────┤ 
│ 현재 비밀번호 *                   │
│ ┌─────────────────────────┐     │
│ │ ••••••••••••            │     │
│ └─────────────────────────┘     │
│                                 │
│ 새 비밀번호 *                     │
│ ┌─────────────────────────┐     │
│ │ ••••••••••••            │     │
│ └─────────────────────────┘     │
│ 8자 이상, 영문+숫자 조합          │
│                                 │ 
│ 새 비밀번호 확인 *                │
│ ┌─────────────────────────┐     │
│ │ ••••••••••••            │     │
│ └─────────────────────────┘     │
├─────────────────────────────────┤
│ ┌─────────────────────────┐     │
│ │ 변경                     │    │
│ └─────────────────────────┘     │
└─────────────────────────────────┘
```
**연결 API**: `PATCH /members/me/password`

**동작 상세**:
- 현재 비밀번호, 새 비밀번호, 새 비밀번호 확인 모두 필수
- 새 비밀번호와 확인 값이 다르면 버튼 비활성 또는 인라인 오류 표시
- 변경 성공 시 "비밀번호가 변경되었습니다." 토스트 후 마이페이지 복귀

**에러 처리**:
- 현재 비밀번호 불일치 → "현재 비밀번호가 올바르지 않습니다."
- 새 비밀번호 정책 불일치 → "비밀번호는 8자 이상, 영문+숫자 조합이어야 합니다."
- 새 비밀번호 확인 불일치 → "새 비밀번호가 일치하지 않습니다."

---
## S09. 내 판매 목록

```
┌─────────────────────────────────┐
│  ← 내 판매 목록                  │
├─────────────────────────────────┤
│  [판매중(2)] [예약중(1)] [완료(5)] │
├─────────────────────────────────┤
│ ┌──────────────────────────────┐ │
│ │[이미지] 침대 프레임           │ │
│ │        150,000원 · 판매중    │ │
│ │        [수정] [삭제]         │ │
│ └──────────────────────────────┘ │
│ ┌──────────────────────────────┐ │
│ │[이미지] 책상                 │ │
│ │        50,000원 · 예약중     │ │
│ │        [거래시작] [예약취소] │ │
│ └──────────────────────────────┘ │
└─────────────────────────────────┘
```

**연결 API**: `GET /members/me/products` (내 판매 목록), `PATCH /trades/{id}/status` (거래시작 · 예약취소)

---

## S05. 상품 수정

```
┌─────────────────────────────────┐
│  ← 상품 수정                [완료] │
├─────────────────────────────────┤
│  사진 (현재 2장 / 최대 10장)     │
│  ┌──┐ ┌──┐ ┌──┐               │
│  │📷│ │📷│ │ +│               │
│  └──┘ └──┘ └──┘               │
│  (기존 사진 X 버튼으로 삭제)     │
├─────────────────────────────────┤
│  카테고리                        │
│  [가구       ▼] [침대/매트리스▼] │
├─────────────────────────────────┤
│  제목 *                          │
│  ┌─────────────────────────┐    │
│  │ 침대 프레임 퀸사이즈     │    │
│  └─────────────────────────┘    │
├─────────────────────────────────┤
│  가격 *                          │
│  ┌─────────────────────────┐    │
│  │ 150,000            원   │    │
│  └─────────────────────────┘    │
│  ⚠️ 예약중/거래중 상태에서는      │
│     가격 수정이 불가합니다        │
├─────────────────────────────────┤
│  설명                            │
│  ┌─────────────────────────┐    │
│  │ 2년 사용, 상태 양호...   │    │
│  └─────────────────────────┘    │
└─────────────────────────────────┘
```

**연결 API**:
- 초기 데이터: `GET /products/{productId}`
- 수정 완료: `PATCH /products/{productId}`
- 에러: RESERVED·TRADING 상태에서 가격 수정 시 `400 INVALID_REQUEST` 토스트

---

## S06. 채팅방 목록

```
┌─────────────────────────────────┐
│  채팅                             │
├─────────────────────────────────┤
│ ┌──────────────────────────────┐ │
│ │[썸네일] 김민준                │ │
│ │ 침대 프레임 퀸사이즈          │ │
│ │ 아직 판매 중인가요?   10분 전 │ │
│ │                    [미읽 2]  │ │
│ └──────────────────────────────┘ │
│ ┌──────────────────────────────┐ │
│ │[썸네일] 박준서                │ │
│ │ 소파 3인용                   │ │
│ │ 네, 내일 거래 가능합니다  1시간│ │
│ └──────────────────────────────┘ │
│ ┌──────────────────────────────┐ │
│ │[썸네일] 이수지                │ │
│ │ 원피스 S사이즈                │ │
│ │ 거래 완료! 감사합니다   어제  │ │
│ └──────────────────────────────┘ │
├─────────────────────────────────┤
│ 🏠홈  🔍검색  ✉️채팅  👤마이        │
└─────────────────────────────────┘
```

**연결 API**: `GET /chat-rooms`
**응답 필드**: chatRoomId, 상대방 닉네임, 상품명, 마지막 메시지 미리보기(30자), 미읽음 수, last_message_at

---

## S10. 내 찜 목록

```
┌─────────────────────────────────┐
│  ← 내 찜 목록                   │
├─────────────────────────────────┤
│  총 12개                         │
├─────────────────────────────────┤
│ ┌──────────────────────────────┐ │
│ │[이미지] 아이패드 Pro 11인치   │ │
│ │        200,000원 · 판매중    │ │
│ │        ♥ 찜 취소             │ │
│ └──────────────────────────────┘ │
│ ┌──────────────────────────────┐ │
│ │[이미지] 원피스 S              │ │
│ │        30,000원 · 예약중     │ │
│ │        ♥ 찜 취소             │ │
│ └──────────────────────────────┘ │
│ ┌──────────────────────────────┐ │
│ │[이미지] 자전거                │ │
│ │        판매완료              │ │
│ │        ♥ 찜 취소             │ │
│ └──────────────────────────────┘ │
└─────────────────────────────────┘
```

**연결 API**: `GET /members/me/likes`
**찜 취소**: `DELETE /products/{productId}/likes` → 목록에서 즉시 제거

---

## S11. 로그인

```
┌─────────────────────────────────┐
│                                  │
│         🏠 GetChu                │
│         중고거래 플랫폼           │
│                                  │
├─────────────────────────────────┤
│  이메일                          │
│  ┌─────────────────────────┐    │
│  │ user@example.com        │    │
│  └─────────────────────────┘    │
│                                  │
│  비밀번호                        │
│  ┌─────────────────────────┐    │
│  │ ••••••••                │    │
│  └─────────────────────────┘    │
│                                  │
│  ┌─────────────────────────┐    │
│  │        로그인            │    │
│  └─────────────────────────┘    │
│                                  │
│  계정이 없으신가요? [회원가입 →]  │
│                                  │
└─────────────────────────────────┘
```

**연결 API**: `POST /auth/login`
**에러 처리**:
- `401 INVALID_CREDENTIALS` → "이메일 또는 비밀번호가 올바르지 않습니다." 인라인 표시
- 로그인 성공 → AccessToken 저장 후 이전 화면 또는 홈으로 이동

---

## S12. 회원가입

```
┌─────────────────────────────────┐
│  ← 회원가입                      │
├─────────────────────────────────┤
│  이메일 *                        │
│  ┌─────────────────────────┐    │
│  │ user@example.com        │    │
│  └─────────────────────────┘    │
│                                  │
│  비밀번호 * (8자 이상, 영문+숫자) │
│  ┌─────────────────────────┐    │
│  │ ••••••••                │    │
│  └─────────────────────────┘    │
│                                  │
│  비밀번호 확인 *                  │
│  ┌─────────────────────────┐    │
│  │ ••••••••                │    │
│  └─────────────────────────┘    │
│                                  │
│  닉네임 *                        │
│  ┌─────────────────────────┐    │
│  │ 민준                    │    │
│  └─────────────────────────┘    │
│  프로필 이미지                   │
│  ┌─────────────────────────┐    │
│  │                         │    │
│  └─────────────────────────┘    │
│  ┌─────────────────────────┐    │
│  │        회원가입          │    │
│  └─────────────────────────┘    │
└─────────────────────────────────┘
```

**연결 API**: `POST /auth/signup`
**에러 처리**:
- `409 DUPLICATE_EMAIL` → 이메일 필드 아래 "이미 사용 중인 이메일입니다." 인라인 표시
- `400 INVALID_REQUEST` → 각 필드 아래 유효성 오류 메시지 표시
- 성공 → 자동 로그인 후 홈으로 이동

---

## S13. 리뷰 작성

```
┌─────────────────────────────────┐
│  ← 리뷰 작성                     │
├─────────────────────────────────┤
│  거래 상품                        │
│  ┌──────────────────────────────┐│
│  │[이미지] 침대 프레임 퀸사이즈  ││
│  │        150,000원             ││
│  └──────────────────────────────┘│
├─────────────────────────────────┤
│  판매자: 김민준                   │
├─────────────────────────────────┤
│  거래는 어떠셨나요?               │
│                                  │
│       ☆  ☆  ☆  ☆  ☆           │
│       ★  ★  ★  ★  ☆  (4점)   │
├─────────────────────────────────┤
│  상세 후기                        │
│  ┌─────────────────────────┐    │
│  │ 친절하게 거래해주셨어요. │    │
│  │ 물건 상태도 설명과 동일  │    │
│  │ 했습니다 :)             │    │
│  └─────────────────────────┘    │
│  (최대 200자)                    │
├─────────────────────────────────┤
│  ┌─────────────────────────┐    │
│  │      리뷰 등록           │    │
│  └─────────────────────────┘    │
│  * 등록 후 수정/삭제 불가합니다  │
└─────────────────────────────────┘
```

**연결 API**: `POST /trades/{tradeId}/reviews`
**에러 처리**:
- `403 FORBIDDEN` → 해당 거래의 구매자가 아닌 경우 (진입 자체 차단)
- `409` → 이미 리뷰 작성 완료 시 "이미 작성한 리뷰가 있습니다." 토스트 후 이전 화면 이동
- 성공 → "리뷰가 등록되었습니다." 토스트 후 홈으로 이동

---

## 화면별 API 매핑 요약 (전체 13개)

| 화면 | 주요 API 호출 |
|------|--------------|
| S01 홈 | `GET /products` |
| S02 검색 | `GET /search/popular`, `GET /products/search` |
| S03 상품 상세 | `GET /products/{id}`, `POST /products/{id}/likes`, `POST /products/{id}/reserve` |
| S04 상품 등록 | `GET /categories`, `POST /products` |
| S05 상품 수정 | `GET /products/{id}`, `PATCH /products/{id}` |
| S06 채팅방 목록 | `GET /chat-rooms` |
| S07 채팅방 상세 | `GET /chat-rooms/{id}/messages` + WebSocket STOMP |
| S08 마이페이지 | `GET /members/me`, `POST /auth/logout` |
| S09 내 판매 목록 | `GET /members/me/products`, `PATCH /trades/{id}/status` |
| S10 내 찜 목록 | `GET /members/me/likes`, `DELETE /products/{id}/likes` |
| S11 로그인 | `POST /auth/login` |
| S12 회원가입 | `POST /auth/signup` |
| S13 리뷰 작성 | `POST /trades/{tradeId}/reviews` |

---

## Figma 목업 가이드

> 바이브코딩 팀이 Figma 목업 제작 시 참고할 디자인 시스템

**컬러 팔레트**:
- Primary: `#FF6F0F` (당근 오렌지 계열)
- Background: `#FFFFFF`
- Surface: `#F5F5F5`
- Text Primary: `#1A1A1A`
- Text Secondary: `#767676`
- Error: `#E53E3E`

**타이포그래피**:
- 제목: 18px Bold
- 본문: 14px Regular
- 캡션: 12px Regular

**컴포넌트 우선순위**:
1. 상품 카드 (목록, 검색에서 반복 사용)
2. 하단 탭바 (4탭: 홈/검색/채팅/마이)
3. 상태 배지 (판매중/예약중/거래중/판매완료)
4. 채팅 버블 (좌측: 상대방, 우측: 나)
