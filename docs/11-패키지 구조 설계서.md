# 11. 백엔드 패키지 구조 설계서

> **버전**: v1.2
> **패키지 루트**: `com.clone.carrot`
> **설계 원칙**: 도메인형(Domain-Driven) + 계층형(Layered) 혼합 구조
>
> **핵심 설계 결정 요약**
>
> | 항목 | 결정 | 이유 |
> |------|------|------|
> | 구조 방식 | 도메인형 최상위 + 계층형 내부 | 도메인 간 결합도 최소화, 기능 집중성 확보 |
> | 카테고리 구조 | 단층(depth 없음) | 대분류/소분류 계층 없이 단일 카테고리 목록으로 단순화 |
> | 락 전용 Facade 분리 | `TradeLockFacade` | `@Transactional` ↔ Redisson Lock 생명주기 충돌 방지 |
> | QueryDSL Custom 분리 | `ProductRepositoryCustom` | 동적 쿼리를 JPA Repository와 분리하여 관심사 명확화 |
> | WebSocket Config 위치 | `global/config` | 인프라 설정은 도메인이 아닌 전역 관심사로 분류 |

---

## 1. 전체 패키지 트리

```
src/main/java/com/clone/carrot/
│
├── global/                         # 앱 전체 공통 관심사
│   ├── config/                     # 인프라 · 프레임워크 설정
│   ├── security/                   # JWT 인증 필터 체인
│   ├── exception/                  # 공통 예외 정의 · 핸들러
│   ├── common/                     # 공통 응답 DTO
│   └── util/                       # 범용 유틸리티
│
└── domain/                         # 비즈니스 도메인
    ├── auth/                       # 인증 (회원가입 · 로그인 · 토큰)
    ├── member/                     # 회원 프로필 · 마이페이지
    ├── category/                   # 카테고리 목록 조회 (단층)
    ├── product/                    # 매물 CRUD · 검색 · 좋아요
    ├── trade/                      # 거래 예약 · 상태 전이 · 동시성 제어
    ├── chat/                       # 실시간 채팅 (WebSocket/STOMP)
    └── review/                     # 거래 후 리뷰 · 매너온도
```

---

## 2. `global` 패키지 상세

```
global/
│
├── config/
│   ├── WebConfig.java              # CORS, MVC 설정
│   ├── SecurityConfig.java         # Spring Security 필터 체인, 공개 URL 화이트리스트
│   ├── RedisConfig.java            # RedisTemplate, RedissonClient 빈 등록
│   ├── WebSocketConfig.java        # STOMP 브로커 · 엔드포인트 (/ws-chat) 등록
│   ├── QuerydslConfig.java         # JPAQueryFactory 빈 등록
│   └── CacheConfig.java            # Caffeine 캐시 매니저 설정 (popularKeywords, TTL 10분)
│
├── security/
│   ├── JwtProvider.java            # 토큰 생성 · 검증 · Claim 추출
│   ├── JwtAuthFilter.java          # OncePerRequestFilter — Authorization 헤더 파싱
│   ├── JwtAuthEntryPoint.java      # 401 Unauthorized 응답 처리
│   ├── JwtAccessDeniedHandler.java # 403 Forbidden 응답 처리
│   └── CustomUserDetails.java      # UserDetails 구현체 (memberId, email, role 보관)
│
├── exception/
│   ├── GlobalExceptionHandler.java # @RestControllerAdvice — 전역 예외 → ErrorResponse 변환
│   ├── BusinessException.java      # 비즈니스 예외 최상위 (code + message)
│   ├── NotFoundException.java      # 404 — 리소스 없음
│   ├── ConflictException.java      # 409 — 중복 · 이미 예약됨 (ALREADY_RESERVED 포함)
│   ├── ForbiddenException.java     # 403 — 권한 없음
│   ├── InvalidRequestException.java# 400 — 입력 유효성 실패
│   └── LockTimeoutException.java   # 409 — DB 락 대기 타임아웃 (LOCK_TIMEOUT)
│
├── common/
│   ├── ApiResponse.java            # 공통 응답 래퍼 { data, code, message }
│   ├── ErrorResponse.java          # 에러 응답 { code, message, timestamp, errors[] }
│   └── CursorPageResponse.java     # 커서 페이징 응답 { content[], nextCursor, hasNext }
│
└── util/
    ├── SecurityUtil.java           # SecurityContextHolder에서 memberId 추출 헬퍼
    └── CursorUtil.java             # Cursor 값 Base64 인코딩 · 디코딩
```

---

## 3. `domain` 패키지 상세

### 3-1. `auth` — 인증

```
domain/auth/
├── controller/
│   └── AuthController.java         # POST /auth/signup · login · logout
├── dto/
│   ├── request/
│   │   ├── SignupRequest.java       # { email, password, nickname } + @Valid 제약
│   │   └── LoginRequest.java        # { email, password }
│   └── response/
│       ├── SignupResponse.java      # { id, email, nickname }
│       └── LoginResponse.java       # { accessToken, tokenType, expiresIn }
└── service/
    └── AuthService.java            # 회원가입(BCrypt 해시) · 로그인(JWT 발급) · 로그아웃
```

> **토큰 전략**: AccessToken 15분 단독 (RefreshToken 미사용)

---

### 3-2. `member` — 회원 프로필

```
domain/member/
├── controller/
│   └── MemberController.java       # GET · PATCH /members/me
├── dto/
│   ├── request/
│   │   └── UpdateMemberRequest.java # { nickname } — 수정 가능 필드만 포함
│   └── response/
│       └── MemberProfileResponse.java # { id, nickname, averageRating, reviewCount }
├── entity/
│   └── Member.java                 # JPA 엔티티 (email UK, BCrypt password, averageRating)
├── repository/
│   └── MemberRepository.java       # JpaRepository<Member, Long>
└── service/
    └── MemberService.java          # 프로필 조회 · 수정, averageRating + reviewCount 업데이트 (리뷰 작성 시 호출)
```

---

### 3-3. `category` — 카테고리 (단층)

계층 구조 없이 단일 카테고리 목록으로 단순화. `parent_id`, `depth` 컬럼 없음.

```
domain/category/
├── controller/
│   └── CategoryController.java     # GET /categories
├── dto/
│   └── response/
│       └── CategoryResponse.java   # { id, name }
├── entity/
│   └── Category.java               # JPA 엔티티 (id, name) — 자기 참조 없음
├── repository/
│   └── CategoryRepository.java     # JpaRepository<Category, Long>
└── service/
    └── CategoryService.java        # 전체 카테고리 목록 반환
```

---

### 3-4. `product` — 매물 ⭐

복잡한 동적 검색(QueryDSL)과 캐싱(Redis Cache-Aside, Caffeine)이 집중된 도메인.

```
domain/product/
├── controller/
│   ├── ProductController.java      # GET /products · /products/{id} · POST · PATCH · DELETE
│   ├── ProductSearchController.java# GET /products/search · /search/popular
│   └── LikeController.java         # POST · DELETE /products/{id}/likes
│                                   # GET /members/me/likes
├── dto/
│   ├── request/
│   │   ├── CreateProductRequest.java  # { title, description, price, categoryId, imageUrls[] }
│   │   ├── UpdateProductRequest.java  # 부분 수정 — null 필드는 변경 없음
│   │   └── ProductSearchRequest.java  # { keyword, categoryId, status, cursor, size }
│   └── response/
│       ├── ProductSummaryResponse.java # 목록용 { id, title, price, thumbnailUrl, status, likeCount }
│       ├── ProductDetailResponse.java  # 상세용 + seller 정보 + imageUrls[]
│       └── PopularKeywordResponse.java # { keywords[], cachedAt }
├── entity/
│   ├── Product.java                # 매물 엔티티 (status, likeCount 비정규화 컬럼 포함)
│   ├── ProductImage.java           # PRODUCT_IMAGE (image_url, sort_order)
│   ├── Like.java                   # LIKE (member_id, product_id UNIQUE)
│   └── KeywordLog.java             # KEYWORD_LOG (keyword, count, log_date)
├── enums/
│   └── ProductStatus.java          # SALE | RESERVED | TRADING | SOLD | REVIEWED | DELETED
├── repository/
│   ├── ProductRepository.java          # JpaRepository — 기본 CRUD
│   ├── ProductRepositoryCustom.java    # [Interface] QueryDSL 동적 쿼리 계약
│   ├── ProductRepositoryImpl.java      # [Class] BooleanBuilder 기반 동적 검색 구현
│   │                                   #   + Cursor 페이지네이션 (createdAt DESC, id DESC)
│   ├── ProductImageRepository.java     # JpaRepository<ProductImage, Long>
│   ├── LikeRepository.java             # JpaRepository<Like, Long>
│   │                                   #   existsByMemberIdAndProductId() 포함
│   └── KeywordLogRepository.java       # 최근 7일 TOP 10 집계 네이티브 쿼리
└── service/
    ├── ProductService.java         # 매물 등록 · 수정 · 삭제 + Redis Cache-Aside (ADR-008)
    │                               #   수정·삭제 시 redisTemplate.delete("product:{id}") eviction
    ├── ProductSearchService.java   # 검색 + 검색어 로그 비동기 저장
    │                               #   @Cacheable("popularKeywords") → Caffeine (ADR-003)
    ├── PopularKeywordScheduler.java# @Scheduled(fixedRate=600000) — KEYWORD_LOG 집계 후 캐시 PUT
    └── LikeService.java            # 좋아요/취소 + like_count ±1 (허용 오차 비정규화)
```

> **QueryDSL Custom 패턴**:
> `ProductRepository`가 `ProductRepositoryCustom`을 상속하면, Spring Data가 `ProductRepositoryImpl`을 자동으로 주입한다. 복잡한 동적 쿼리와 JPA 기본 CRUD가 단일 Repository 인터페이스로 노출됨.

---

### 3-5. `trade` — 거래 · 동시성 제어 ⭐⭐

동시성 제어(Redisson + 비관적 락)와 상태 전이 규칙이 집중된 핵심 도메인.

```
domain/trade/
├── controller/
│   └── TradeController.java        # POST /products/{id}/reserve
│                                   # PATCH /trades/{id}/status
│                                   # GET   /trades/{id}
├── dto/
│   ├── request/
│   │   └── TradeStatusUpdateRequest.java  # { status } — "TRADING" | "SOLD" | "SALE"(취소)
│   └── response/
│       └── TradeResponse.java      # { tradeId, productId, status }
├── entity/
│   └── Trade.java                  # JPA 엔티티 (product_id FK, buyer_id, seller_id, status)
│                                   #   정적 팩토리: Trade.create(product, buyerId)
├── repository/
│   └── TradeRepository.java        # JpaRepository
│                                   #   existsByProductIdAndStatusIn() — 활성 거래 중복 확인
│                                   #   existsByProductIdAndBuyerIdAndStatusNot() — 동일 구매자 중복
├── enums/
│   └── TradeStatus.java            # RESERVED | TRADING | SOLD | REVIEWED | CANCELLED
└── service/
    ├── TradeLockFacade.java        # ⭐ Redisson 분산락 전용 Facade (@Component, 트랜잭션 없음)
    │                               #   tryLock(wait=0, lease=3s) → reserveWithPessimisticLock 호출
    │                               #   Redis 장애 시 fallback: 비관적 락 단독 처리
    ├── TradeService.java           # [Interface]
    └── TradeServiceImpl.java       # [Class] @Transactional
                                    #   reserveWithPessimisticLock — SELECT FOR UPDATE + 상태 검증
                                    #   changeStatus — RESERVED→TRADING→SOLD / 취소 상태 전이 검증
                                    #   PRODUCT.status ↔ TRADE.status 동기화 (단일 트랜잭션)
```

> **Facade 분리 이유 (면접 포인트)**:
>
> ```
> [잘못된 구조]                        [올바른 구조]
> TradeService {                       TradeLockFacade {           ← 락 담당 (트랜잭션 없음)
>   @Transactional                       lock.tryLock()
>   reserve() {                          tradeService.reserve()    ← 내부에서 @Transactional
>     lock.tryLock()                     lock.unlock()             ← COMMIT 이후 해제 보장
>     ... DB 처리 ...               }
>     lock.unlock()  ← 문제!         TradeServiceImpl {           ← DB 처리 담당
>   }                                  @Transactional
> }                                    reserve() { ... }
>                                    }
>
> 문제: COMMIT 전에 unlock → 다른 스레드가 아직 반영 전 DB 상태를 읽음
> 해결: Facade가 트랜잭션 외부에서 lock/unlock 생명주기를 감싸도록 분리
> ```

---

### 3-6. `chat` — 실시간 채팅

```
domain/chat/
├── controller/
│   ├── ChatRoomController.java     # POST /chat-rooms (생성 or 기존 반환)
│   │                               # GET  /chat-rooms (내 채팅방 목록)
│   │                               # GET  /chat-rooms/{id}/messages (REST 이전 메시지 조회)
│   └── ChatMessageController.java  # @MessageMapping("/chat.sendMessage") — STOMP 수신 처리
│                                   #   → /topic/room.{chatRoomId} 브로드캐스트
├── dto/
│   ├── request/
│   │   ├── CreateChatRoomRequest.java  # { productId }
│   │   └── ChatMessageRequest.java     # { chatRoomId, content } — STOMP 메시지 페이로드
│   └── response/
│       ├── ChatRoomResponse.java        # { chatRoomId, created } (200: 기존, 201: 신규)
│       ├── ChatRoomSummaryResponse.java # 목록용 { 상대방닉네임, 상품명, 마지막메시지, 미읽음수 }
│       └── ChatMessageResponse.java     # { messageId, senderId, content, createdAt }
├── entity/
│   ├── ChatRoom.java               # CHAT_ROOM (product_id, buyer_id, seller_id)
│   │                               #   UNIQUE(buyer_id, product_id) — 중복 채팅방 방지
│   └── ChatMessage.java            # CHAT_MESSAGE (chat_room_id, sender_id, content, is_read)
├── repository/
│   ├── ChatRoomRepository.java     # findByBuyerIdAndProductId() — 중복 채팅방 조회
│   │                               # findByBuyerIdOrSellerId() — 내 채팅방 목록
│   └── ChatMessageRepository.java  # findByChatRoomIdOrderByIdDesc() — Cursor 페이지네이션
└── service/
    ├── ChatRoomService.java        # 채팅방 생성(멱등) · 목록 조회
    └── ChatMessageService.java     # 메시지 저장 (MySQL) · 참여자 검증 · 브로드캐스트
```

> **WebSocket 설정 위치**: `global/config/WebSocketConfig.java`
> - STOMP 브로커 엔드포인트(`/ws-chat`), SockJS 폴백, JWT 핸드셰이크 인터셉터 등록
>
> **메시지 저장 전략**: Redis Pub/Sub이 아닌 MySQL 직접 저장 (ADR-005)
> - 재연결 시 REST API(`GET /chat-rooms/{id}/messages`)로 이전 메시지 재조회 가능
> - Cursor 기준: `messageId DESC`

---

### 3-7. `review` — 리뷰

```
domain/review/
├── controller/
│   └── ReviewController.java       # POST /trades/{tradeId}/reviews
│                                   # GET  /members/{memberId}/reviews
├── dto/
│   ├── request/
│   │   └── CreateReviewRequest.java  # { rating(1~5), content }
│   └── response/
│       └── ReviewResponse.java       # { id, rating, content, reviewerNickname, createdAt }
├── entity/
│   └── Review.java                 # REVIEW (trade_id UK, reviewer_id, reviewee_id, rating)
├── repository/
│   └── ReviewRepository.java       # existsByTradeId() — 거래당 1건 제한
│                                   # findByRevieweeId() — 판매자 리뷰 목록
└── service/
    └── ReviewService.java          # 리뷰 저장 + MemberService.updateAverageRating() 호출
                                    # 사전 검증: 거래 구매자 본인 여부, SOLD 상태 여부, 중복 여부
```

---

## 4. 전체 파일 수 요약

| 패키지 | Controller | Service | Repository | Entity | DTO(req/res) | 합계 |
|--------|-----------|---------|------------|--------|--------------|------|
| global | - | - | - | - | 3 | ~23개 |
| auth | 1 | 1 | - | - | 4 | 6개 |
| member | 1 | 1 | 1 | 1 | 2 | 6개 |
| category | 1 | 1 | 1 | 1 | 1 | 5개 |
| product | 3 | 3+1(스케줄러) | 5 | 4+1(enums) | 6 | 23개 |
| trade | 1 | 3(Facade 포함) | 1 | 1+1(enums) | 2 | 9개 |
| chat | 2 | 2 | 2 | 2 | 5 | 13개 |
| review | 1 | 1 | 1 | 1 | 2 | 6개 |
| **합계** | **10** | **13** | **11** | **10** | **25** | **~81개** |

---

## 5. 주요 Bean 의존 관계

```
[요청 처리 흐름 — 예약 API]

HTTP POST /products/{id}/reserve
  └─▶ TradeController
        └─▶ TradeLockFacade          ← Redisson Lock 획득 (wait=0, lease=3s)
              └─▶ TradeServiceImpl   ← @Transactional 시작
                    ├─▶ ProductRepository.findByIdWithLock()   ← SELECT FOR UPDATE
                    ├─▶ Product.changeStatus(RESERVED)
                    ├─▶ TradeRepository.save(trade)
                    └─▶ @Transactional COMMIT
              └─▶ TradeLockFacade    ← finally: lock.unlock()  (COMMIT 이후)

[인기 검색어 캐시 흐름]

@Scheduled(10분)
  └─▶ PopularKeywordScheduler
        ├─▶ KeywordLogRepository     ← 최근 7일 TOP 10 집계
        └─▶ CacheManager.put("popularKeywords", top10)   ← Caffeine 갱신

GET /search/popular
  └─▶ ProductSearchController
        └─▶ ProductSearchService
              └─▶ @Cacheable("popularKeywords")           ← HIT: 즉시 반환 / MISS: DB 집계 후 PUT

[상품 상세 캐시 흐름]

GET /products/{id}
  └─▶ ProductService.getProduct()
        ├─▶ redisTemplate.get("product:{id}")  ← HIT: 즉시 반환
        └─▶ ProductRepository.findById()       ← MISS: DB 조회 후 Redis PUT (TTL 10분)

PATCH /products/{id}
  └─▶ ProductService.updateProduct()
        └─▶ redisTemplate.delete("product:{id}")          ← Cache Eviction
```